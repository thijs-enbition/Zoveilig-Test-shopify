# "Eerste 3 maanden" as the package at quantity 3 (2026-09-23)

Branch `fix/prepay-qty3-discount-2026-09-23`. It replaces the prepaid PROMO product model of
`docs/prepay-promo-lineitems-2026-09-22.md` (PR #93).

## The model

A package purchase is exactly two cart lines:

| Line | Quantity | Price |
|---|---|---|
| Installation product: Climax `CL003`, or the NAMI option the customer picks | 1 | full price, never discounted |
| The real package product (e.g. `N0002` Zeker) | 3 (`promo.months`) | 50% off, applied by Shopify |

The 50% comes from the existing automatic discount "Eerste 3 maanden". The theme computes no
discount, creates no products and never changes the discount. From month 4 Odoo bills the
plain monthly rate over SEPA. See **Pre-live-push checks** below: this part is not confirmed yet.

**Hard rule (Thijs, 2026-09-23): never create products.** The only products sold on this site
are the ones in the table below. Odoo's contract automation keys off these products, and every
new Shopify product becomes a stub product in Odoo. Never modify products or the discount from
this repo either.

## Products and discount on 2026-09-23 (verified via Admin API)

| Package | Handle | SKU | Price | Variant ID | Install |
|---|---|---|---|---|---|
| Inzicht | `langer-thuis-inzicht` | N0001 | €19,95 | 58726375555453 | NAMI |
| Zeker | `langer-thuis-zeker` | N0002 | €24,95 | 58446173143421 | NAMI |
| Alert | `mijn-thuis-alert` | N0003 | €24,95 | 58726375620989 | NAMI |
| Beschermd | `langer-thuis-beschermd` | LT-BES | €39,95 | 58446173176189 | Climax |
| Protect | `mijn-thuis-protect` | MT-PRO | €37,95 | 58446173274493 | Climax |

| Installation | Handle | SKU | Price | Variant ID |
|---|---|---|---|---|
| NAMI · Geen installatie | `geen-installatie-nami` | N0006 | €0 | 58726375719293 |
| NAMI · Telefonische ondersteuning | `telefonische-ondersteuning-installatie-nami` | N0005 | €35 | 58726375391613 |
| NAMI · Installatie aan huis | `installatie-nami` | N0004 | €99 | 58726375489917 |
| Climax · Climax Instalatie | `climax-instalatie` | CL003 | €99 | 58726375588221 |

The theme resolves all of these by handle (`all_products[handle]`), never by variant ID.

**Today's product swap (Odoo sync):**
- Inzicht and Alert are new products under the same handles. The old ones are archived as
  `langer-thuis-inzicht-oud-lt-inz` (LT-INZ) and `mijn-thuis-alert-oud-mt-ale` (MT-ALE).
- Zeker kept its variant; only its SKU changed (LT-ZEK → N0002).
- The NAMI install SKUs changed: 020103 / 03012026 / 04012026 became N0006 / N0005 / N0004. The
  old telefonisch product is archived as `telefonische-ondersteuning-installatie-nami-oud-03012026`.
- The old Climax install product (`activatie-en-installatie`, ACT-INSTALL) no longer exists. Its
  replacement is `climax-instalatie` / CL003 (confirmed by Thijs). It is for Beschermd and Protect
  only and must never be added to a NAMI cart.
- Product titles now start with "Nami …" (e.g. "Nami Langer Thuis Zeker").
- The swap dropped `custom.finder_key` from the new Inzicht and Alert products. The planning chat
  set it back the same day (`aware`, `secure`).
- The swap switched inventory tracking on for N0001 and N0003, with 0 in stock. See the checks
  below.

**Discount "Eerste 3 maanden":**
- automatic, amount off products, 50%, ACTIVE since 2026-09-16, no end date, combines with nothing;
- scope: exactly the products `langer-thuis-inzicht`, `langer-thuis-zeker`, `mijn-thuis-alert`,
  `langer-thuis-beschermd`, `mijn-thuis-protect` and `mijn-thuis-vista`, i.e. N0001, N0002, N0003,
  LT-BES, MT-PRO and MT-VIS. It was updated to the new Inzicht/Alert products;
- no installation product is in scope.

## Why the PROMO-* approach was abandoned

PR #93 (merged 2026-09-22) sold the promo as five separate products, "Eerste 3 maanden korting —
<pakket>" (`eerste-3-maanden-<pakket>`, SKUs PROMO-INZ/ZEK/BES/ALE/PRO). The plain package product
never went into the cart.

Every new Shopify product becomes a stub product in Odoo. **A `PROMO-ZEK` stub now exists in Odoo
and is left there for Odoo-side cleanup** (outside this repo). The five products were deleted from
Shopify on 2026-09-23. On `main` that left every add-to-cart flow adding nothing, because each one
still looked up its promo product. The store is password-protected, so no rollback was needed.

This branch removes every PROMO-* / `eerste-3-maanden-*` reference and the `promoProduct` config
fields. It also removes the `zv_promo_live` theme setting: the Shopify discount is now the only
switch. `check_pricing.py` check #9 fails if any of these come back.

## Confirmed price change

Alert €19,95 → **€24,95** and Protect €34,95 → **€37,95** (Thijs, 2026-09-23), matching the live
Shopify products. `kosten.xlsx` is being updated to match.

## Rounding: the config follows Shopify

**Shopify rounds per unit, not per line.** It prices each unit of the package line on its own
(50% of €24,95 = €12,475 → €12,48), then multiplies by 3: €37,44. The first version of this
branch rounded the line as a whole (50% of €74,85 = €37,425 → €37,43), which was 1 cent below
what Shopify charges for every package whose 50% lands on a half cent.

**Decision (Thijs, 2026-09-23): follow Shopify.**
- `build_pricing.py` now computes the prepaid amount as `months × ROUND_HALF_UP(monthly × 50%)`.
- The indicative contract value uses the same figure: `installation + prepaid amount + monthly ×
  (term − 3)`, and `recurringMonthsBilled` is `term − 3`.
- The actie page, `dueToday` and the cart now all show exactly what Shopify charges.

| Package | Prepaid 3 months | Vandaag: geen / telefonisch / aan huis | Contract value |
|---|---|---|---|
| Inzicht (12 mnd) | €29,94 | €29,94 / €64,94 / €128,94 | €308,49 |
| Zeker (12 mnd) | €37,44 | €37,44 / €72,44 / €136,44 | €360,99 |
| Alert (12 mnd) | €37,44 | €37,44 / €72,44 / €136,44 | €360,99 |
| Beschermd (36 mnd) | €59,94 | — / — / €158,94 | €1.477,29 |
| Protect (36 mnd) | €56,94 | — / — / €155,94 | €1.408,29 |

The contract value is for "aan huis"; Overzicht swaps in the NAMI option actually chosen. The
was-prices are unchanged: €59,85 / €74,85 / €74,85 / €119,85 / €113,85. All 11 "vandaag"
amounts were measured in real carts and match to the cent (see **Validation**).

## Pre-live-push checks (Thijs)

1. **Odoo billing** (Thijs with Alex; `unresolved.ODOO_PROMO_DISCOUNT`). An order line like
   `N0002 × 3` must become:
   - **exactly one contract at the plain monthly rate** (€24,95/mnd), not a contract at
     quantity 3 (€74,85/mnd) and not three contracts;
   - with **SEPA starting at month 4**, not month 1, so months 1–3 are not charged twice.

   Neither is confirmed yet. This doesn't block the branch or its validation.

Resolved the same day:
- **Inventory:** tracking is off on N0001 and N0003 (re-checked read-only 2026-09-23: all 9 sold
  products are `tracked: false` and available).
- **Rounding:** the config follows Shopify (above).

## How the theme enforces the model

- **Add-to-cart** (Oplossingen cards and "Meer informatie" modal, Vergelijk pakketten, the
  embedded package matcher):
  - adds the package variant at `promo.packageCartQuantity` (3);
  - adds CL003 only for a Climax package (`installGroup` from the pricing config);
  - never adds a package that is already in the cart, since that would add months.
- **Package identity** is read from the package line:
  - Liquid: `product.type` + `custom.finder_key`;
  - the Cart AJAX guard: product type + handle, because it can't see metafields.
- **"Daarna €X/mnd"** is the package line's unit price before the discount
  (`item.original_price`), never unit × 3.
- **Cart guard** (`snippets/zv-cart-guard.liquid`, on `/cart` and Overzicht) fixes one thing per
  load, then reloads:
  - package line → quantity 3;
  - duplicate package line → removed;
  - installation line → quantity 1;
  - Climax package without CL003 → CL003 added;
  - CL003 in a cart without a Climax package → removed.

  It never picks a NAMI option for the customer.
- **Checkout gate** (Overzicht, server-side) disables checkout while any of these is true:
  - `cart_needs_activation`: installation line missing;
  - `cart_needs_fix`: anything the guard is still fixing;
  - `cart_pkg_undiscounted`: a package line with no discount at all, e.g. if the Shopify
    discount is ever deactivated. The guard can't fix that, so checkout stays blocked with
    "Er ging iets mis met uw bestelling. Neem contact op via 088 122 11 11." **A customer can
    never pay 3 full months.**
- **Quantity controls:** package and installation lines on `/cart` have no +/−. The add-to-cart
  drawer only has "remove".
- **Package names** always come from `pricing.config.json`, never the Shopify product title:
  Odoo renamed the products ("Nami Langer Thuis Zeker") and can do it again. Each package in
  the config has a `finderKey`, and the build emits `zv_package_names_by_fk`. One snippet,
  `snippets/zv-package-name.liquid`, maps a product's `custom.finder_key` to its config name.
  It is used by:
  - the Oplossingen cards (heading and `data-package-name`);
  - the pakket-matcher (products JSON, the advice bar, the comparison table and its "Kies …"
    buttons);
  - `/cart` (item name and summary row);
  - Overzicht (item name and breakdown row).

  The cart drawer takes the name from the package's card on the page. The product title is
  only a fallback for an unknown finder_key.
- **GA4:** `item_id` comes from the line's own SKU (`assets/zv-track.js`, and the SKU property
  on add-to-cart), so it reports N0001/N0002/N0003 from the live cart with no code change.

## Validation (2026-09-23)

### Round 1 (first version, half-up per line)

- `check_pricing.py` passes: all 11 "vandaag te betalen" amounts, `promoWasCents` (5985 / 7485 /
  7485 / 11985 / 11385), SKUs, handles, and no `promoProduct` / PROMO-* / `zv_promo_live`.
- `shopify theme check --fail-level error`: 0 errors.
- Pushed to disposable theme `zz-validate-prepay-qty3-2026-09-23` (#189080338813); Shopify's
  server-side validator accepted every file. Theme deleted afterwards.
- **Real carts** through `shopify theme dev`'s local proxy, for the 5 buyable combinations:
  - the package line is at qty 3 with the right SKU (N0002 / LT-BES / MT-PRO);
  - the only discount allocation is "Eerste 3 maanden", on the package line;
  - the install line is undiscounted at qty 1 and is the right product (CL003 only for Climax);
  - the Overzicht breakdown rows add up to the cart total to the cent;
  - "Daarna" is the unit price.
- **Blocking tests** on Overzicht: wrong quantity, CL003 in a NAMI cart, a stray CL003, Beschermd
  without CL003, and a duplicate package line are all blocked. A two-package cart is not blocked
  and sums correctly.
- **Cart guard JS**, the real script from the rendered page run against live carts:
  - qty 2 → 3 and qty 5 → 3, with the install line reset to 1;
  - CL003 added to Beschermd, and removed from NAMI carts (with and without a NAMI choice);
  - duplicate package line removed;
  - a correct cart is left alone.
- **Headless Chrome click-through:**
  - the Oplossingen cards and the comparison table's "Kies …" add the right lines, and a second
    click adds nothing;
  - the modal's "Direct afrekenen" goes to Overzicht;
  - the NAMI radio choice unblocks checkout;
  - `/cart` shows no quantity controls on package and installation lines;
  - the actie page shows "Eerste 3 maanden: ~~€ 74,85~~ € 37,43 eenmalig vandaag · Daarna
    € 24,95/mnd" and the exact disclosure text.
- **PROMO products:** `/products/eerste-3-maanden-*.js` returns 404 for all five, and no rendered
  page mentions them.
- **Not validated:** the 6 Inzicht/Alert combinations (sold out). Also the undiscounted-line block
  in a real cart, which would mean deactivating the discount, and that's not allowed. It was
  checked by reading the code only.

### Round 2 (per-unit rounding, config names, new copy)

- `check_pricing.py` passes. It asserts the prepaid amounts (2994 / 3744 / 3744 / 5994 / 5694),
  all 11 `dueToday` amounts, the unchanged was-prices, and the contract values (Zeker 36099;
  Inzicht 30849, Alert 36099, Beschermd 147729 and Protect 140829 by the same rule).
- `shopify theme check --fail-level error`: 0 errors.
- Pushed to disposable theme `zz-validate-prepay-qty3-b-2026-09-23` (#189081125245); every file
  was accepted. Theme deleted afterwards.
- **Real carts for all 11 combinations**, Inzicht and Alert included (174 checks, 0 failures).
  Each cart total equals the table above to the cent. In each one:
  - the package line is at qty 3 with its N000x / LT-BES / MT-PRO SKU, and it carries the only
    discount allocation;
  - the installation line is undiscounted at qty 1;
  - the breakdown rows add up to the cart total;
  - the Overzicht item row, the breakdown row ("Inzicht × 3 — eerste 3 maanden (50% korting)")
    and the `/cart` item and summary show the config name, never "Nami …".
- **Headless Chrome:**
  - the Oplossingen cards, both matchers, both comparison tables (Oplossingen and
    /pages/vergelijk-pakketten), the "Kies …" buttons and the drawer all show Inzicht / Zeker /
    Beschermd / Alert / Protect;
  - "Nami" appears nowhere on Oplossingen;
  - the actie page shows €29,94 / €37,44 / €59,94 / €37,44 / €56,94, which equals Shopify;
  - Overzicht shows the new explainer copy.

## Copy changes for review

Overzicht explainer texts (schema defaults in `sections/zv-checkout-overview.liquid`;
`templates/cart.overzicht.json` stores no override, so the defaults are what customers see).
Both described the PROMO model.

**"Toelichting · vandaag te betalen"** (`due_today_text`, shown under "Opbouw van dit bedrag")
- Before: *Vandaag betaalt u de eenmalige kosten voor activatie en installatie plus de
  vooruitbetaalde korting voor de eerste 3 maanden, via de beveiligde Shopify-checkout.*
- After: *Vandaag betaalt u de eventuele installatiekosten en de eerste 3 maanden van uw pakket
  vooruit, met 50% korting, via de beveiligde Shopify-checkout.*

**"Toelichting · promo"** (`before_explainer`, shown at the bottom of the cost card)
- Before: *De korting van de eerste 3 maanden rekent u vandaag ineens af, naast de
  installatiekosten. Uw maandbedrag zelf verandert niet. Alle bedragen zijn inclusief btw (21%).*
- After: *De eerste 3 maanden van uw pakket betaalt u vandaag vooruit, met 50% korting, samen met
  de eventuele installatiekosten. Daarna betaalt u het vaste maandbedrag via SEPA-incasso. Alle
  bedragen zijn inclusief btw (21%).*

"eventuele installatiekosten" covers NAMI "Geen installatie" (€0) as well as Climax. Both texts
can be overridden per store in the theme editor without a code change.

## Found but not fixed (outside this brief)

- **Installation product titles.** Odoo renamed the installation products too ("Nami Geen
  Instalatie", "Nami Instalatie Telefoon", "Nami Installatie Locatie", "Climax Instalatie"). The
  Overzicht breakdown uses the config labels for them, but the `/cart` and Overzicht item rows,
  the `/cart` summary and the cart drawer still show these titles, including the misspellings.
  The package-name fix could be extended to them the same way.
- GA4 `item_name` (`assets/zv-track.js`) is the Shopify product title, so it now reports "Nami
  Langer Thuis Zeker"; `item_id` (the SKU) is correct. The Oplossingen "Meer informatie" modal
  uses its own literal design-copy names (`#opl-modal-data`); they match the config today but
  aren't read from it.
- **NAMI install de-duplication** in the guard keeps the *oldest* of two NAMI install lines, not
  the newest its comment says (`/cart.js` lists newest first). This was already the case before
  this change.
- A NAMI cart before the customer's install choice shows "We herstellen dit automatisch", which
  it doesn't. Also pre-existing.
- The GA4 `add_to_cart` event sends quantity 1 at the monthly price, not 3 at today's amount. The
  purchase value is the real cart total.
- No store page uses the `actie` template (`templates/page.actie.json`), so the actie page isn't
  reachable on the storefront yet. It was validated via `?view=actie`.
- `pricing/pricing.schema.json` is stale (e.g. it requires `contractTerm`) and nothing enforces it.
- Three old `zz-validate-*` themes from earlier sessions are still on the store.
- Two snippets this branch deletes (`zv-package-monthly-cents`, `zv-package-promo-variant`) stay
  on the live theme when it is pushed per file with `--nodelete`. They're unused, and Thijs
  asked to leave them there for now.
